# 🎫 VOEV Scanner

Автономное веб-приложение для сканирования и валидации билетов с прямым подключением к Supabase.

## 🚀 Быстрый старт

### Установка зависимостей
```bash
npm install
```

### Настройка переменных окружения
Создайте файл `.env.local`:
```bash
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
```

### Запуск в режиме разработки
```bash
npm run dev
```

Приложение будет доступно по адресу [http://localhost:3000](http://localhost:3000)

### Сборка для продакшн
```bash
npm run build
npm start
```

## 📱 Возможности

- **🔗 Прямое подключение к Supabase** - Без зависимости от основного сервера
- **📱 QR сканирование** - Использование камеры устройства
- **✅ Валидация билетов** - Проверка и обновление статуса в реальном времени
- **📊 Статистика** - Live данные о сканированиях
- **🔊 Звуковые уведомления** - Успех/ошибка при сканировании
- **🎨 Адаптивный дизайн** - Работает на всех устройствах
- **⚙️ Настройка ворот** - Выбор входа (Gate-1, Gate-2, VIP, Staff)
- **🔄 Автономная работа** - Не требует основного сервера

## 🛠️ Технологии

- **Next.js 15** - React фреймворк
- **TypeScript** - Типизированный JavaScript
- **Tailwind CSS** - Utility-first CSS фреймворк
- **Supabase** - Backend и база данных
- **html5-qrcode** - Библиотека для QR сканирования
- **Lucide React** - Иконки
- **React Hot Toast** - Уведомления

## 📂 Структура проекта

```
src/
├── app/
│   ├── api/scan/          # API для работы с билетами
│   ├── globals.css        # Глобальные стили
│   ├── layout.tsx         # Корневой layout
│   └── page.tsx          # Главная страница сканнера
├── components/
│   ├── QRScanner.tsx     # Компонент QR сканнера
│   ├── ScanResult.tsx    # Отображение результатов сканирования
│   └── ScannerStats.tsx  # Статистика сканирований
├── lib/
│   └── supabase.ts       # Supabase клиент
└── types/
    └── html5-qrcode.d.ts # Типы для html5-qrcode
```

## 🔧 Настройка

### Переменные окружения

| Переменная | Описание | Пример |
|-----------|----------|--------|
| `NEXT_PUBLIC_SUPABASE_URL` | URL вашего проекта Supabase | `https://xxx.supabase.co` |
| `NEXT_PUBLIC_SUPABASE_ANON_KEY` | Анонимный ключ Supabase | `eyJhbGciOiJIUzI1NiIs...` |

### Структура базы данных Supabase

Сканнер работает с таблицами:
- `tickets` - билеты с QR кодами
- `orders` - заказы с информацией о владельцах
- `seats` - места (если есть)
- `zones` - зоны с ценами

### Row Level Security (RLS)

Убедитесь что в Supabase настроены правильные RLS политики для доступа к таблицам билетов.

## 📱 Развертывание

### Vercel (рекомендуется)
```bash
# Установите Vercel CLI
npm i -g vercel

# Деплой
vercel

# Добавьте переменные окружения
vercel env add NEXT_PUBLIC_SUPABASE_URL
vercel env add NEXT_PUBLIC_SUPABASE_ANON_KEY
```

### Netlify
```bash
# Сборка
npm run build

# Загрузите папку .next на Netlify
# Не забудьте добавить переменные окружения
```

### Docker
```dockerfile
FROM node:18-alpine

WORKDIR /app
COPY package*.json ./
RUN npm install

COPY . .
RUN npm run build

EXPOSE 3000
CMD ["npm", "start"]
```

### Собственный сервер
```bash
# Запуск с PM2
pm2 start npm --name "voev-scanner" -- start

# Настройка Nginx (см. DEPLOYMENT.md)
```

## 🔒 Безопасность

- **Прямое подключение к Supabase** - Без промежуточных серверов
- **RLS политики** - Контроль доступа на уровне базы данных
- **HTTPS обязателен** - Для доступа к камере
- **Анонимный ключ** - Безопасный доступ только к нужным данным

## 📋 Требования к браузеру

- **Chrome/Edge**: Версия 90+
- **Firefox**: Версия 88+
- **Safari**: Версия 14+
- **Мобильные браузеры**: Поддержка WebRTC и Camera API

## 🚫 Ограничения

- Требуется HTTPS для доступа к камере
- Требует интернет соединения для Supabase
- Не работает в приватном режиме некоторых браузеров

## 🔄 API Endpoints

### POST /api/scan
Сканирование билета
```json
{
  "qr_code": "ticket-qr-code",
  "gate_info": "Gate-1",
  "scanner_id": "Scanner-Web-001"
}
```

### GET /api/scan
Получение статистики
```json
{
  "total": 150,
  "valid": 20,
  "used": 120,
  "invalid": 5,
  "cancelled": 3,
  "pending": 2,
  "recent_scans": [...]
}
```

## 🐛 Отладка

### Проблемы с камерой
1. Проверьте разрешения браузера
2. Убедитесь что используется HTTPS
3. Проверьте что камера не используется другим приложением

### Проблемы с Supabase
1. Проверьте URL и ключи в `.env.local`
2. Убедитесь что RLS политики настроены
3. Проверьте подключение к интернету

### Проблемы с QR кодами
1. Убедитесь что QR код содержит правильный формат
2. Проверьте что билет существует в базе данных
3. Проверьте структуру таблицы `tickets`

## 🎯 Преимущества

✅ **Автономность** - Не зависит от основного сервера VOEV  
✅ **Производительность** - Прямые запросы к базе данных  
✅ **Надежность** - Меньше точек отказа  
✅ **Простота** - Легкое развертывание и настройка  
✅ **Масштабируемость** - Множественные экземпляры сканнеров  

## 📞 Поддержка

Если у вас возникли проблемы с сканнером:
1. Проверьте консоль браузера на наличие ошибок
2. Убедитесь что Supabase подключение работает
3. Проверьте структуру базы данных

## 📄 Лицензия

© 2025 VOEV. Все права защищены.